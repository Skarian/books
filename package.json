{
  "name": "books",
  "version": "1.0.0",
  "private": true,
  "description": "Neil's self-hosted Books service glue",
  "scripts": {
    "check": "node --check src/*.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=22"
  }
}
