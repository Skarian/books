{
  "name": "books",
  "version": "1.0.0",
  "private": true,
  "description": "Neil's self-hosted Books service glue",
  "scripts": {
    "check": "node --check src/*.js test/*.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=22"
  }
}
